"""Persistence layer: one repository module per aggregate.

- ``job_repository``   — job posts and their skills
- ``run_repository``   — agent runs and their per-step traces
- ``match_repository`` — structured resume-vs-job match results
"""
